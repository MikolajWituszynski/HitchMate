server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/hitchhikers
    username: meek
    password: password
  jpa:
    hibernate:
        ddl-auto: create-drop
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  main:
    web-application-type: servlet